#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

/**
 * printf - writes the character c to stdout
 * @format: The character to print
 * Return: On success 1.
 */
int printf(const char *format, ...)
{
printf("Hello!\n");
exit(EXIT_SUCCESS);
}
