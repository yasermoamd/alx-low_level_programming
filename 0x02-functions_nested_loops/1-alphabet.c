#include "main.h"
/**
 * main function start the program
 *
 *
 * return nothing if its successful
 *
 */
void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}	
	return (0);
}
